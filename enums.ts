//枚举
(function () {
    enum Direction {
        Up = 1,
        Down,
        Left,
        Right
    }
    const enum Directions {
        Up,
        Down,
        Left,
        Right
    }

    let a=Directions.Up;
  
console.log(Direction);
})();